generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
}

model SiteData {
  id           Int      @id @default(1)
  json_content Json
  updated_at   DateTime @default(now()) @updatedAt
}
